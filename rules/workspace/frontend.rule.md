# Workspace Rule: Frontend Engineering Standards

- Functional components with React 19 / TypeScript.
- Server Components by default; isolate `'use client'` to leaf components.
- Colocate state and use custom hooks with `AbortController` cancellation support.
- Apply WCAG AA accessible contrast and keyboard focus indicators.
