# AutoHotkey v2 Project Rules

- Strict AutoHotkey v2 syntax only; never mix legacy v1 commands.
- Scope all hotkeys strictly using #HotIf directives.
- Wrap risky operations in try/catch blocks with global error handlers.
- DPI-aware, clean dark-mode GUIs without emoji clutter.
